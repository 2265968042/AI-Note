# 刷完 LeetCode Hot 100 后，我真正需要记住什么？

## ——一份写给未来自己的算法面试复习手册

最近终于把 **LeetCode Hot 100** 全部刷完了。

不仅仅是 AC，我基本也给每一道题做了输出，把自己的理解重新写了一遍。

但刷到后面，我越来越明显地意识到一个问题：

**刷完 100 道题，并不代表半年以后还能做出这 100 道题。**

有些题刚写完的时候觉得已经完全理解了，隔两周再看，还是会卡住；有些代码看起来熟悉，但一旦关掉题解，让自己从空白编辑器开始写，边界、初始化、递归返回值又会出问题。

所以真正值得留下来的，并不是：

> 第 1 题怎么写，第 2 题怎么写……第 100 题怎么写。

而应该是：

> **这 100 道题最终能不能被压缩成十几个可以迁移的解题模型。**

以后面试碰到一道没见过的题，我不可能在脑海里搜索：

> “这是不是我刷过的第 73 道题？”

真正有用的是看到题目以后能够迅速判断：

> 这是滑动窗口？  
> 这是二分边界？  
> 这是回溯？  
> 这是树形递归？  
> 这是动态规划？  
> 还是本质上只是一个快慢指针或者位运算技巧？

所以这篇文章不是 Hot 100 的 100 道标准题解。

它更像是我刷完这 100 道题之后，为未来的自己留下的一份：

**算法面试恢复手册。**

---

# 一、拿到一道题，我现在应该先想什么？

以前我看到一道算法题，很容易直接进入代码。

比如看到数组，就开始：

```python
for i in range(n):
```

看到二分，就开始：

```python
left = 0
right = n - 1
```

看到动态规划，就开始：

```python
dp = [...]
```

后来我越来越觉得，这个顺序是反的。

真正应该先做的是：

> **先把题目翻译成一个算法模型，再写代码。**

我现在会先观察下面这些信号：

| 题目特征             | 第一反应            |
| ---------------- | --------------- |
| 查某个东西是否出现过、出现几次  | 哈希表             |
| 连续子数组、连续子串       | 滑动窗口 / 前缀和      |
| 有序数组             | 二分 / 双指针        |
| 所有排列、组合、路径       | 回溯              |
| 最短步数、最少层数        | BFS             |
| 树、图、连通区域         | DFS / BFS       |
| 第 K 大、Top K      | 堆 / Quickselect |
| 下一个更大/更小元素       | 单调栈             |
| 环、中点、重复节点        | 快慢指针            |
| 最大值、最小值、方案数、可不可达 | 动态规划 / 贪心       |
| 每个物品选或不选         | 0-1 背包          |
| 一个物品可以反复使用       | 完全背包            |

当然，这张表不是万能答案。

真正重要的是培养一种意识：

**先判断结构，再写代码。**

尤其是动态规划、二分、回溯这三类题，如果一开始连问题模型都没有定义清楚，直接写代码，往往越写越乱。

---

# 二、数组与哈希：先问自己“我到底需要记住什么信息？”

哈希表是 Hot 100 里非常基础的一类工具。

但我现在不太喜欢把它记成：

> “看到数组就想 HashMap。”

我更愿意记成一个问题：

> **在遍历当前元素的时候，我是不是需要 O(1) 地知道某个信息以前有没有出现过？**

比如：

- 某个值是否出现；

- 某个值出现几次；

- 某个前缀状态是否出现；

- 一个值对应的下标；

- 字符出现的频率。

如果需要，哈希表通常就会出现。

它本质上是在做：

```text
用空间保存历史信息
↓
避免重新向前搜索
```

所以很多原本需要 O(n²) 枚举的问题，可以降到 O(n)。

---

## 1. 前缀和也是同一种思想

前缀和本质上也是：

> **把过去的信息累计下来。**

例如：

```python
prefix[i]
```

表示前 i 个元素的某种累计状态。

那么一个区间的信息就可能变成：

```python
prefix[right] - prefix[left]
```

于是原本每次重新计算区间的问题，就可以 O(1) 得到。

所以以后看到：

> 连续子数组 + 区间和

除了滑动窗口，也应该想到：

> **前缀和。**

如果再加上：

> 有多少个区间满足某个条件

往往还会出现：

> **前缀和 + 哈希表。**

---

## 2. 一个很重要的细节：哈希状态不一定是永久的

我以前容易把 `set`、`dict` 理解成：

> 加进去就一直留着。

但在 DFS、树、回溯这些题里，经常不是这样。

例如当前路径的前缀状态：

```python
prefix[curr] += 1

dfs(node.left)
dfs(node.right)

prefix[curr] -= 1
```

为什么最后还要减回来？

因为这个哈希表记录的不是：

> 整棵树历史上所有出现过的状态。

而是：

> **当前这条 DFS 路径上的状态。**

当这一条路径结束以后，它就应该被撤销。

这一点其实和后面的回溯是同一个思想：

> **递归进入之前改变状态，递归回来以后恢复状态。**

---

# 三、双指针与滑动窗口：真正维护的是“区间性质”

以前看滑动窗口模板，会很容易注意：

```python
left
right
```

但真正重要的其实不是这两个变量。

而是：

> **`[left, right]` 之间到底保持着什么性质？**

---

## 1. 相向双指针

例如一个有序数组：

```text
left ---------------- right
```

我们通过当前两端的关系，判断：

> 哪一边一定不可能产生答案？

然后排除掉它。

所以双指针的核心不是：

> 同时维护两个下标。

而是：

> **利用单调性，一次排除一批不可能答案。**

---

## 2. 滑动窗口

滑动窗口更典型。

基本结构可以记：

```python
left = 0

for right in range(len(nums)):

    # 把 nums[right] 加入窗口

    while 窗口不满足条件:

        # 移除 nums[left]
        left += 1

    # 更新答案
```

但真正需要回答的是三个问题：

### right 为什么扩大？

因为我要尝试加入新的元素。

### left 什么时候收缩？

因为当前窗口已经不满足要求。

### 收缩的时候怎么维护窗口状态？

例如：

```python
count[c] -= 1
```

或者：

```python
window_sum -= nums[left]
```

所以我以后复习滑动窗口，不需要死背哪一道题。

只需要先问：

> **我的窗口里到底维护了什么信息？**

---

# 四、链表：不要把链表当成数组来想

链表题是我前期刷题时花时间比较多的一类。

后来发现，链表最重要的能力并不是什么复杂算法，而是：

> **能够准确追踪指针。**

数组里：

```python
nums[i]
```

位置关系天然存在。

但链表里：

```python
cur.next
```

一旦修改，原来的链路就可能直接断掉。

所以链表题我现在有一个习惯：

> **先画指针，再写代码。**

---

## 1. 反转链表为什么一定要保存 next？

经典写法：

```python
prev = None
cur = head

while cur:
    nxt = cur.next
    cur.next = prev
    prev = cur
    cur = nxt

return prev
```

这里最重要的不是模板本身。

而是：

```python
nxt = cur.next
```

为什么必须放在：

```python
cur.next = prev
```

之前？

因为一旦执行：

```python
cur.next = prev
```

原来的下一个节点就丢了。

所以链表题经常需要：

> **修改指针之前，先保存后路。**

---

## 2. Dummy Node 为什么这么常见？

链表里：

```python
dummy = ListNode(0)
dummy.next = head
```

经常出现。

它的意义是：

> **把头节点变成一个普通节点。**

没有 dummy：

```text
删除头节点
删除中间节点
```

可能需要两套逻辑。

有 dummy：

```text
删除节点 = 修改前驱的 next
```

统一了。

所以以后看到：

- 删除；

- 合并；

- 插入；

- 重排；

可以先想一下：

> 要不要 dummy？

---

## 3. 快慢指针不是一个题，而是一整个模型

最基础的是：

```python
slow = slow.next
fast = fast.next.next
```

它可以解决：

- 链表中点；

- 判断环；

- 找环入口；

- 链表分段；

- 甚至“寻找重复数”这种看起来根本不是链表的题。

关键是意识到：

> **快慢指针解决的是“沿着一个确定 next 关系不断移动”的问题。**

这个 next 不一定真的是：

```python
node.next
```

也可能是：

```python
nums[i]
```

---

## 4. 链表归并排序真正难的是边界

我以前在链表归并里反复纠结过：

```text
[head, tail)
```

这样的区间到底什么意思。

后来发现这类题真正难的往往不是“归并排序”四个字，而是：

> **递归函数到底负责哪一段？**

如果定义：

```text
sort(head, tail)
```

处理：

```text
[head, tail)
```

那么 `tail` 本身是不属于当前区间的。

所以递归终止、断链、找中点，都必须围绕这个定义。

这和二分是一样的：

> **先定义区间，再写边界。**

不要反过来。

---

# 五、树：递归函数到底返回什么？

刷树题以后，我觉得最重要的一个问题是：

> **`dfs(node)` 到底返回什么？**

以前写递归，很容易只想：

> 什么时候结束？

但其实在写终止条件之前，更应该先定义：

```python
def dfs(node):
```

这个函数的语义。

它可能返回：

- 当前节点的最大深度；

- 当前子树是否满足条件；

- 当前节点对应的最优值；

- 一个节点；

- 当前路径状态。

只有这句话定义清楚，递归才真正清楚。

---

## 1. 二叉树直径：递归返回值和最终答案不是一回事

这是一个非常典型的例子。

题目问：

> 二叉树的最大直径。

但是：

```python
dfs(node)
```

并不直接返回整棵子树的最大直径。

它更适合返回：

> **从当前节点向下走的最大深度。**

因为父节点需要这个信息。

假设：

```text
left_depth
right_depth
```

那么经过当前节点的直径可以计算：

```text
left_depth + right_depth
```

于是：

```python
ans = max(ans, left_depth + right_depth)
```

但函数返回：

```python
return max(left_depth, right_depth) + 1
```

这里我后来才真正理解：

> **递归函数返回给父节点的信息，不一定就是题目最终要求的信息。**

这在树形 DP 中特别重要。

---

## 2. BST 为什么经常想到中序遍历？

二叉搜索树有一个非常重要的性质：

> **中序遍历得到有序序列。**

所以看到：

- BST 第 K 小；

- 验证 BST；

- BST 中元素大小关系；

应该立刻想到：

```text
左 → 根 → 右
```

也就是中序遍历。

不是因为“这道题背过”，而是因为：

> BST 的结构性质和中序遍历天然匹配。

---

## 3. Python 递归里的 self

前期我还经常在：

```python
self.xxx
```

和普通局部变量之间混乱。

一个比较实用的理解是：

```python
class Solution:
```

只是定义了一个类。

真正执行时，会有一个：

```python
Solution()
```

实例。

这个实例就是：

```python
self
```

所以：

```python
self.ans
```

可以理解为：

> 当前这个 Solution 对象保存的状态。

递归过程中调用：

```python
self.dfs(...)
```

始终还是同一个对象。

因此：

```python
self.ans
```

可以在不同递归层之间共享。

而如果只是当前递归内部需要的临时变量，就没必要放到 `self` 上。

---

# 六、DFS、BFS 与图：先想“深度”还是“层”

DFS 和 BFS 很容易被记成：

```text
DFS = 递归
BFS = 队列
```

但我现在更愿意从搜索结构理解。

---

## 1. DFS

DFS 是：

> **一条路先走到底。**

所以很适合：

- 连通分量；

- 岛屿；

- 树递归；

- 路径搜索；

- 回溯。

空间复杂度更接近：

```text
搜索深度
```

---

## 2. BFS

BFS 是：

> **一层一层扩张。**

所以当题目出现：

- 最少步数；

- 最近距离；

- 第几层；

- 最短路径；

应该优先想到 BFS。

BFS 的队列里通常维护的是：

> 当前或下一层所有可能状态。

---

## 3. visited 到底什么时候 remove？

这是我以前很容易混乱的地方。

例如岛屿：

```python
visited.add((i, j))
```

通常不会 remove。

因为：

> 这个格子已经属于某个连通区域，以后没必要再访问。

但是单词搜索里：

```python
visited.add((i, j))

dfs(...)

visited.remove((i, j))
```

为什么又要撤销？

因为单词搜索里的 `visited` 表示：

> **当前路径已经使用的格子。**

当前路径结束以后，这个格子在另一条路径里仍然可以使用。

所以一个非常重要的判断是：

> **visited 表示全局历史，还是当前路径状态？**

如果是全局历史，不撤销。

如果是当前路径，通常要回溯。

---

## 4. break 不能结束所有递归

这也是一个曾经容易混淆的问题。

```python
break
```

只结束：

> 当前这一层的循环。

它不会自动把所有递归层全部退出。

如果需要某个结果一路向上传递，通常应该：

```python
if dfs(...):
    return True
```

然后每一层都通过：

```python
return True
```

继续向上传播。

---

# 七、回溯：for 管当前层，递归管下一层

回溯是我刷题过程中花时间理解最多的一类模型之一。

真正理解以后，我觉得它可以压缩成一句话：

> **回溯就是在一棵决策树上做 DFS。**

---

## 1. 最重要的模板

```python
def backtrack(...):

    if 终止条件:
        ans.append(...)
        return

    for choice in choices:

        # 做选择
        path.append(choice)

        # 进入下一层
        backtrack(...)

        # 撤销选择
        path.pop()
```

这几行代码看起来简单，但里面其实有三个不同层次。

### for

负责：

> 当前这一层有哪些选择。

### 递归

负责：

> 做完这个选择以后，下一层怎么选。

### pop / remove

负责：

> 当前分支探索完成以后，把状态恢复。

---

## 2. 为什么递归回来以后 for 会继续？

以前我会产生一种疑问：

假设：

```text
1 → 2 → 3
```

已经走完。

回溯到：

```text
1 → 2
```

以后，为什么不会又重新：

```text
1 → 2 → 3
```

走一次？

因为递归返回以后：

> **for 循环的位置没有重置。**

例如：

```python
for i in range(...):
```

当前已经执行完：

```python
i = 2
```

递归返回以后，会继续：

```python
i = 3
```

而不是重新从头执行。

所以：

> **回溯恢复的是状态，不是程序执行位置。**

这个理解非常重要。

---

## 3. 谁 add，谁 remove

单词搜索里我曾经把：

```python
visited.remove(...)
```

放到父节点去处理。

后来发现一个很实用的原则：

> **谁修改状态，谁负责恢复状态。**

例如：

```python
visited.add((i, j))

...

visited.remove((i, j))
```

应该在同一个逻辑层级。

同样：

```python
path.append(x)
backtrack(...)
path.pop()
```

也是一个完整组合。

---

## 4. used、visited、begin 到底有什么区别？

它们表面上都是“防止重复”，但语义不同。

### used

表示：

> 当前排列路径中，这个元素是否已经使用。

全排列：

```python
if used[i]:
    continue
```

---

### begin

表示：

> 下一层从哪个位置开始选择。

例如组合、子集：

```python
for i in range(begin, n):
```

这样就不会产生：

```text
[1,2]
[2,1]
```

这种顺序不同但本质相同的组合。

---

### visited

通常表示：

> 当前搜索路径里哪些状态已经占用。

例如单词搜索。

所以以后不要只记：

```text
回溯要有 visited。
```

而应该问：

> **我到底需要限制什么？**

---

## 5. Hot 100 里的回溯题，其实只是约束不同

例如：

### 全排列

每个元素只能用一次。

所以需要：

```python
used
```

### 子集

每个元素选或者不选。

核心是：

```python
begin
```

### 电话号码字母组合

每一层对应一个数字。

每一层选择：

```text
这个数字对应的某个字母
```

### 组合总和

元素可以重复使用。

所以递归时仍然可以传：

```python
i
```

而不是：

```python
i + 1
```

### 括号生成

不是所有选择都合法。

规则：

```text
left < n      可以加 (
right < left 可以加 )
```

### N 皇后

额外维护：

```text
列
主对角线
副对角线
```

所以回溯题真正应该复习的是：

> **基础决策树 + 题目增加了什么约束？**

而不是背八套代码。

---

# 八、二分：最重要的不是 mid，而是“我到底在找什么”

二分是另一个我前期经常被：

```text
<
<=
mid
mid + 1
mid - 1
```

搞乱的地方。

后来我发现根本原因是：

> **在写代码之前，没有先定义答案。**

---

## 1. 二分之前先说一句中文

例如：

> 我要找第一个 `>= target` 的位置。

那么：

```python
nums[mid] >= target
```

时，说明：

> `mid` 有可能就是答案。

所以应该继续向左找。

---

如果目标是：

> 第一个 `> target` 的位置。

那么“等于 target”也应该被排除到左边。

于是条件就会发生变化。

所以：

```text
< 还是 <=
```

并不是模板要求。

它取决于：

> **等于 target 的元素应该属于哪一边。**

---

## 2. lower_bound 模板

我现在更愿意统一成：

```python
def lower_bound(nums, target):
    left = -1
    right = len(nums)

    while left + 1 < right:
        mid = (left + right) // 2

        if nums[mid] >= target:
            right = mid
        else:
            left = mid

    return right
```

这里可以理解成：

```text
left  一定 < target
right 一定 >= target
```

最后：

```text
left + 1 == right
```

那么 `right` 就是：

> 第一个 `>= target`。

这个模板之所以好用，是因为：

> **左右边界的语义一直不变。**

---

## 3. 为什么有时候等于 target 也不能 return？

例如普通搜索：

```python
if nums[mid] == target:
    return mid
```

没问题。

因为题目只是：

> 找到任意一个 target。

但 searchRange 要找：

> 第一个 target。

即使：

```python
nums[mid] == target
```

也不能确定它是不是最左边。

所以还要继续往左。

这让我后来意识到：

> **二分查找不等于“找到 target”。**

更准确地说：

> 二分是在一个具有单调性的答案空间里寻找边界。

---

## 4. 为什么有时候 right = mid，而不是 mid - 1？

例如寻找旋转数组最小值：

```python
while left < right:
    mid = (left + right) // 2

    if nums[mid] <= nums[right]:
        right = mid
    else:
        left = mid + 1
```

这里：

```python
right = mid
```

是因为：

> `mid` 自己仍然有可能就是最小值。

所以不能把它排除。

而：

```python
left = mid + 1
```

是因为这个分支已经能够确定：

> `mid` 不可能是答案。

所以可以直接扔掉。

因此一个非常重要的判断标准是：

> **mid 有没有可能是答案？**

有可能：

```text
保留 mid
```

不可能：

```text
mid ± 1
```

---

# 九、栈与单调栈：栈里保存的是“还没有处理完的状态”

普通栈其实比较直观。

例如有效括号：

```python
stack.append("(")
```

表示：

> 有一个左括号还没等到对应的右括号。

所以当右括号出现时：

```python
stack.pop()
```

完成匹配。

---

## 1. Decode String 让我更理解“栈保存现场”

例如：

```text
3[a2[c]]
```

遇到：

```python
[
```

以后，需要进入下一层字符串解析。

但进去之前必须保存当前层的信息。

所以：

```python
stack.append((cur_str, cur_num))
```

实际上保存的是：

> **等我从下一层回来以后，恢复当前层需要的现场。**

这让我意识到：

> 栈里不一定只存一个值。

它可以存：

```python
(prev_str, num)
```

甚至可以存一个完整状态。

---

## 2. 单调栈解决的是“谁还没等到答案”

单调栈常见关键词：

> 下一个更大元素。

例如当前元素还没有找到右边第一个更大的元素，那么它就先进入栈。

未来遇到一个更大的数时：

```python
while stack and nums[stack[-1]] < nums[i]:
    index = stack.pop()
```

说明：

> 当前 `i` 正好解决了这个旧元素的问题。

所以我现在把单调栈理解成：

> **栈中保存的，是一批还没有等到答案的元素。**

至于栈保持递增还是递减，是为了保证：

> 当前元素可以批量解决哪些旧问题。

---

# 十、Top K：先问自己是否真的需要排序

看到：

> 第 K 大

或者：

> 前 K 个高频元素

最直接的方法当然是排序。

但排序得到的信息太多了。

如果我只是想知道：

> 第 K 个位置是什么

其实没必要知道：

```text
第 1 到第 n 个元素的完整顺序。
```

---

## 1. 堆

例如维护一个大小为 K 的小根堆：

```text
始终保留目前最大的 K 个元素
```

那么堆顶就是：

> 这 K 个元素里面最小的那个。

新元素比堆顶大，就替换。

复杂度：

```text
O(n log k)
```

---

## 2. Quickselect

Quickselect 更进一步。

Partition 做的不是完整排序。

它只保证：

```text
pivot 左边满足某种大小关系
pivot 右边满足另一种大小关系
```

至于左边内部：

```text
3,1,2
```

到底有没有顺序，并不重要。

如果 pivot 的最终位置已经告诉我们：

> 第 K 大一定在左边

那就只递归左边。

所以平均复杂度可以达到：

```text
O(n)
```

---

## 3. Partition 不是冒泡排序

这一点我以前也容易混淆。

冒泡排序是：

> 相邻元素之间反复比较、交换。

Partition 是：

> 所有元素和一个 pivot 比较，进行分类。

目标完全不同。

所以 Quickselect 的核心不是：

> “一种更快的排序。”

而是：

> **根本不完成排序，只找到需要的那一部分。**

---

# 十一、贪心：不是“每次选眼前最大”

贪心是一个非常容易被误解的词。

以前我会下意识觉得：

> 贪心 = 当前哪个最大，就选哪个。

但 Jump Game II 让我比较直观地理解了它。

例如：

```text
当前这一跳可以落在多个位置。
```

错误的想法是：

> 直接跳到当前能到达最远的位置。

真正应该考虑的是：

> **这一层所有可能落点中，哪个位置能让下一跳覆盖得最远？**

标准变量：

```python
end = 0
maxposition = 0
step = 0
```

其中：

```text
end
```

表示：

> 当前跳跃次数能够覆盖到的最右边界。

```text
maxposition
```

表示：

> 扫描当前这一层所有位置以后，下一层能够覆盖的最远位置。

当：

```python
i == end
```

说明这一层扫描完成。

于是：

```python
end = maxposition
step += 1
```

这其实很像 BFS：

```text
当前层
↓
扫描当前层所有节点
↓
得到下一层最远边界
```

所以好的贪心不是：

> 盯着眼前最大的数字。

而是：

> **寻找一个可以证明不会让未来更差的局部选择。**

---

# 十二、动态规划：不要先想公式，先定义 dp

动态规划是我刷 Hot 100 后期非常重要的一部分。

我现在觉得 DP 最核心的习惯就是：

> **看到 `dp[i]` 之前，必须先用一句中文说清楚它是什么。**

如果这句话说不清楚，后面状态转移基本一定会乱。

---

## 1. 我现在会按照四步来做 DP

### 第一步：状态定义

例如：

```python
dp[i]
```

到底是：

> 到第 i 个位置？

还是：

> 前 i 个元素？

还是：

> 必须选择第 i 个元素？

---

### 第二步：最后一步做了什么选择？

例如打家劫舍：

对于第 i 间房：

```text
偷
或者
不偷
```

---

### 第三步：写状态转移

如果不偷：

```python
dp[i - 1]
```

如果偷：

```python
dp[i - 2] + nums[i]
```

于是：

```python
dp[i] = max(
    dp[i - 1],
    dp[i - 2] + nums[i]
)
```

---

### 第四步：初始化和遍历顺序

状态依赖谁？

那就必须保证：

> 被依赖的状态先算出来。

---

# 十三、打家劫舍：为什么 dp 的定义这么重要？

我一开始容易写成：

```python
dp[i] = dp[i - 2] + nums[i]
```

这其实隐含了一个错误：

> 默认一定偷第 i 间房。

但题目问的是：

> 最多能偷多少钱。

所以第 i 间房当然可以不偷。

更合理的定义是：

> `dp[i]` 表示考虑到第 i 间房为止，最多能获得多少钱。

于是：

```python
dp[i] = max(
    dp[i - 1],              # 不偷
    dp[i - 2] + nums[i]     # 偷
)
```

初始化也一样。

错误：

```python
dp[1] = nums[1]
```

正确：

```python
dp[1] = max(nums[0], nums[1])
```

因为考虑前两间房，并不意味着一定偷第二间。

所以 DP 有一个非常重要的规律：

> **很多状态转移错误，本质上不是公式不会，而是状态定义一开始就偏了。**

---

# 十四、背包：0-1 背包和完全背包到底差在哪？

以前背包很容易被写成一堆模板。

后来我觉得最值得记的其实只有一句：

### 0-1 背包

> 一个物品最多使用一次。

### 完全背包

> 一个物品可以重复使用。

---

## 1. 分割等和子集为什么是 0-1 背包？

题目：

> 是否能从数组中选择一些数字，使它们的和等于总和的一半。

每个数字：

> 只能选一次。

所以是 0-1 背包。

如果定义：

```text
dp[j]
=
能不能从已经处理过的数字中，选出和为 j 的组合
```

那么：

```python
dp[j] = dp[j] or dp[j - num]
```

关键来了：

```python
for j in range(target, num - 1, -1):
```

为什么容量必须倒序？

因为如果正序：

```text
dp[j - num]
```

可能刚刚已经在当前这一轮被 `num` 更新过。

于是同一个 `num` 会被使用多次。

倒序则保证：

> 当前这一轮读取的仍然是上一轮状态。

这就是 0-1 的本质。

---

## 2. 零钱兑换为什么可以正序？

零钱可以：

> 无限次使用。

于是我们反而允许：

```python
dp[i - coin]
```

已经在这一轮使用过当前 coin。

所以：

```python
for coin in coins:
    for i in range(coin, amount + 1):
```

容量正序。

因此我现在不再单纯死记：

```text
0-1 倒序
完全背包正序
```

而是记：

> **我允不允许当前物品在同一轮再次参与状态转移？**

允许：

```text
正序
```

不允许：

```text
倒序
```

---

# 十五、零钱兑换、完全平方数、单词拆分其实非常像

这几道题表面差异很大。

但抽象以后都是：

> 当前状态能不能由某个更小的状态，加上一个合法选择得到？

---

## 1. 零钱兑换

```python
dp[i]
```

表示：

> 凑出金额 i 的最少硬币数。

那么：

```python
dp[i] = min(
    dp[i],
    dp[i - coin] + 1
)
```

---

## 2. 完全平方数

```python
dp[i]
```

表示：

> 凑出数字 i 最少需要几个完全平方数。

于是：

```python
dp[i] = min(
    dp[i],
    dp[i - j * j] + 1
)
```

---

## 3. 单词拆分

```python
dp[i]
```

表示：

> 字符串前 i 个字符能不能被成功拆分。

寻找一个：

```python
j
```

如果：

```python
dp[j] == True
```

并且：

```python
s[j:i] in wordSet
```

那么：

```python
dp[i] = True
```

虽然一个求最小值，一个求 bool，但思维模式非常接近：

> **找一个前驱状态 + 当前选择。**

---

# 十六、二维字符串 DP：我最需要弄清楚的是“i 到底代表什么”

字符串二维 DP 里，我曾经反复卡在：

```python
dp[i][j]
```

和：

```python
text[i]
```

之间。

关键是：

> **很多二维字符串 DP 中，i 和 j 表示的是字符数量，不是字符下标。**

例如：

```python
dp[i][j]
```

表示：

> text1 的前 i 个字符  
> 和 text2 的前 j 个字符。

所以当前最后一个字符是：

```python
text1[i - 1]
text2[j - 1]
```

而不是：

```python
text1[i]
text2[j]
```

---

# 十七、最长公共子序列：为什么不使用 text1[i-1] 是 dp[i-1][j]？

这一点我之前专门纠结过。

假设：

```python
dp[i][j]
```

表示：

> text1 前 i 个字符和 text2 前 j 个字符的 LCS。

text1 前 i 个字符的下标是：

```text
0,1,2,...,i-1
```

所以：

```python
text1[i - 1]
```

就是最后一个字符。

如果不用它，剩下：

```text
0,1,2,...,i-2
```

一共正好：

```text
i - 1 个字符
```

所以是：

```python
dp[i - 1][j]
```

不是：

```python
dp[i - 2][j]
```

这里最容易混淆的是：

> **“i-1 个字符”和“下标 i-1”完全不是同一个概念。**

---

如果：

```python
text1[i - 1] == text2[j - 1]
```

那么：

```python
dp[i][j] = dp[i - 1][j - 1] + 1
```

如果不相等：

```python
dp[i][j] = max(
    dp[i - 1][j],
    dp[i][j - 1]
)
```

因为最后两个字符无法共同成为新的匹配，只能尝试分别放弃一个。

---

# 十八、最长回文子串：遍历顺序由状态依赖决定

这道题让我更加理解：

> `for i`、`for j` 的顺序并不是固定的。

如果定义：

```python
dp[i][j]
```

表示：

> `s[i:j+1]` 是否是回文串。

那么：

```python
dp[i][j]
```

依赖：

```python
dp[i + 1][j - 1]
```

也就是：

> 一个更短的内部子串。

所以必须保证：

> 短子串先算，大子串后算。

于是非常自然地按长度枚举：

```python
for L in range(2, n + 1):

    for i in range(n - L + 1):

        j = i + L - 1
```

这让我后来对 DP 遍历顺序有了更通用的理解：

> **不是我要从左到右还是从右到左，而是状态依赖决定我怎么遍历。**

---

# 十九、编辑距离：把“最后一步”拆开

编辑距离是我觉得非常适合用来检验二维 DP 是否真正理解的一道题。

定义：

```python
dp[i][j]
```

表示：

> 把 word1 的前 i 个字符变成 word2 的前 j 个字符，最少需要多少次操作。

当前字符：

```python
word1[i - 1]
word2[j - 1]
```

---

如果相等：

```python
word1[i - 1] == word2[j - 1]
```

最后字符已经一样，不需要操作。

所以：

```python
dp[i][j] = dp[i - 1][j - 1]
```

这里我曾经写错成：

```python
dp[i][j] = dp[i - 1][j - 1] + 1
```

其实只要拿：

```text
"a" → "a"
```

一想就知道。

明明 0 次操作。

为什么还要 +1？

---

如果不相等：

### 删除

删掉 word1 最后一个字符：

```python
dp[i - 1][j] + 1
```

### 插入

假设插入 word2 最后一个字符，那么插入之前只需要：

```python
word1 前 i 个字符
→
word2 前 j-1 个字符
```

所以：

```python
dp[i][j - 1] + 1
```

### 替换

把两个最后字符直接替换：

```python
dp[i - 1][j - 1] + 1
```

因此：

```python
dp[i][j] = min(
    dp[i - 1][j] + 1,
    dp[i][j - 1] + 1,
    dp[i - 1][j - 1] + 1
)
```

这道题真正值得记的不是公式。

而是：

> **复杂 DP 可以尝试从“最后一步发生了什么”来拆。**

---

# 二十、一些 Hot 100 技巧题：重要的是那个关键性质

并不是所有题都应该强行归入一个大模型。

有些题只需要记住一个很漂亮的性质。

---

## 1. Single Number：异或

异或：

```python
a ^ a = 0
a ^ 0 = a
```

而且满足交换律和结合律。

所以：

```text
4 ^ 1 ^ 2 ^ 1 ^ 2
```

可以重新排列为：

```text
4 ^ (1 ^ 1) ^ (2 ^ 2)
```

最后：

```text
4
```

代码：

```python
ans = 0

for num in nums:
    ans ^= num

return ans
```

也可以写：

```python
reduce(lambda x, y: x ^ y, nums)
```

`reduce` 的本质就是：

> 把上一次计算结果继续和下一个元素合并。

但面试里我更愿意写 for。

更直观。

---

## 2. Next Permutation：最小幅度地变大

下一个排列可以压缩成三步。

### 第一步

从右向左找第一个：

```python
nums[i] < nums[i + 1]
```

说明：

> i 右边已经处于最大排列。

如果还想整体变大，就必须修改 nums[i]。

### 第二步

从右边找：

> 刚好比 nums[i] 大的数字。

交换。

### 第三步

交换以后，把后缀变成：

> 最小排列。

也就是反转。

所以整个算法本质是：

> **尽量在靠右的位置，进行最小幅度的增大。**

---

## 3. 寻找重复数：数组也可以变成链表

题目中：

```text
nums 长度 = n + 1
nums[i] ∈ [1,n]
```

可以建立：

```text
i → nums[i]
```

例如：

```python
nums = [1,3,4,2,2]
```

形成：

```text
0 → 1 → 3 → 2 → 4
            ↑       ↓
            ←───────
```

数组就变成了一条带环链表。

重复数字对应：

> 环入口。

所以直接使用 Floyd：

```python
slow = nums[0]
fast = nums[nums[0]]

while slow != fast:
    slow = nums[slow]
    fast = nums[nums[fast]]

fast = 0

while slow != fast:
    slow = nums[slow]
    fast = nums[fast]

return slow
```

这道题最值得留下的是：

> **题目表面是数组，但可以通过“值作为下一跳下标”转换成链表模型。**

这种跨模型转换能力，比记代码更重要。

---

# 二十一、我真正值得背下来的模板

100 道完整代码没有必要全部背。

真正应该做到：

> 空白编辑器里能够写出最核心的模板。

---

## 1. 滑动窗口

```python
left = 0

for right in range(len(nums)):

    # 加入右端点

    while 窗口不合法:

        # 移除左端点
        left += 1

    # 更新答案
```

记忆重点：

> 窗口维护什么状态？

---

## 2. 回溯

```python
def backtrack(...):

    if 终止条件:
        ans.append(...)
        return

    for choice in choices:

        做选择

        backtrack(...)

        撤销选择
```

记忆重点：

> for 当前层，递归下一层。

---

## 3. 二分 lower_bound

```python
left = -1
right = len(nums)

while left + 1 < right:

    mid = (left + right) // 2

    if nums[mid] >= target:
        right = mid
    else:
        left = mid

return right
```

记忆重点：

```text
left < target
right >= target
```

---

## 4. 链表反转

```python
prev = None
cur = head

while cur:

    nxt = cur.next

    cur.next = prev

    prev = cur
    cur = nxt

return prev
```

记忆重点：

> 先保存 next。

---

## 5. 树 DFS

```python
def dfs(node):

    if not node:
        return 基础状态

    left = dfs(node.left)
    right = dfs(node.right)

    当前节点处理

    return 给父节点的信息
```

记忆重点：

> dfs 到底返回什么？

---

## 6. BFS

```python
from collections import deque

queue = deque([start])

while queue:

    size = len(queue)

    for _ in range(size):

        cur = queue.popleft()

        for nxt in neighbors(cur):

            if nxt 没访问:
                标记
                queue.append(nxt)
```

记忆重点：

> 队列保存下一层。

---

## 7. 网格 DFS

```python
def dfs(i, j):

    if 越界 or 不满足条件:
        return

    标记当前位置

    for di, dj in directions:
        dfs(i + di, j + dj)
```

记忆重点：

> 标记是永久还是当前路径？

---

## 8. 单调栈

```python
stack = []

for i in range(len(nums)):

    while stack and nums[stack[-1]] < nums[i]:

        index = stack.pop()

        # nums[i] 是 index 的答案

    stack.append(i)
```

记忆重点：

> 栈中是谁还没有找到答案？

---

## 9. 0-1 背包

```python
dp = [False] * (target + 1)
dp[0] = True

for num in nums:

    for j in range(target, num - 1, -1):

        dp[j] = dp[j] or dp[j - num]
```

记忆重点：

> 倒序防止同一个 num 使用多次。

---

## 10. 一维 DP

```python
dp = [...]

初始化

for i in range(...):

    dp[i] = 根据之前状态计算

return dp[-1]
```

看起来几乎没有内容。

但这正好说明：

> DP 没有万能模板。

真正的模板是：

```text
定义状态
↓
找最后一步
↓
状态转移
↓
初始化
↓
确定依赖顺序
```

---

# 二十二、我刷 Hot 100 时真正踩过的 Python 坑

算法理解之外，我还反复犯过一些 Python 层面的错误。

这些错误看起来很小。

但面试里如果发生，就是实打实的 bug。

---

## 1. Python 切片右边不包含

```python
s[i:j]
```

包含：

```text
i ... j-1
```

如果想包含 `j`：

```python
s[i:j + 1]
```

---

## 2. 二维数组不要这样初始化

错误：

```python
dp = [[False] * n] * m
```

因为内部行引用相同。

应该：

```python
dp = [[False] * n for _ in range(m)]
```

---

## 3. `^` 不是乘方

Python：

```python
a ^ b
```

是：

> 按位异或。

乘方：

```python
a ** b
```

平方建议直接：

```python
j * j
```

---

## 4. pop 要调用

错误：

```python
stack.pop
```

这是函数对象。

真正执行：

```python
stack.pop()
```

---

## 5. or 判断不能这么写

错误：

```python
if c == "(" or "[" or "{":
```

实际上几乎永远为 True。

正确：

```python
if c in "([{":
```

---

## 6. Python 负索引不会报错

例如：

```python
nums[-1]
```

合法。

它表示：

> 最后一个元素。

所以零钱兑换里：

```python
dp[i - coin]
```

如果忘记判断：

```python
i >= coin
```

不会一定报错。

可能悄悄读到数组尾部，形成非常隐蔽的 bug。

---

## 7. `List[int]` 不是创建列表

```python
List[int]
```

是：

> 类型提示。

真正创建列表：

```python
[]
```

或者：

```python
list()
```

---

## 8. 类方法递归注意 self

如果定义：

```python
class Solution:

    def dfs(self, ...):
```

调用时通常：

```python
self.dfs(...)
```

`self` 指的是：

> 当前 Solution 实例。

而不是当前函数。

这也是我早期理解 Python 类和递归时比较容易混乱的地方。

---

# 二十三、比代码错误更重要的，是这些思维错误

回头看整个 Hot 100，我觉得真正值得留下来的并不是某一行代码。

而是这些曾经让我卡住的思维方式。

---

## 1. 递归只关注终止条件，却不定义函数语义

以前想：

> “递归什么时候 return？”

现在应该先问：

> **这个函数返回什么信息给上一层？**

---

## 2. 把回溯理解成“递归以后 pop”

其实：

> **回溯的核心是恢复路径状态。**

`pop()` 只是恢复状态的一种方式。

---

## 3. 把二分理解成一套固定模板

于是永远在纠结：

```text
<
<=
left <= right
left < right
mid - 1
mid
```

现在应该先问：

> **我要找哪个边界？左右两边分别代表什么？**

---

## 4. DP 一上来先想状态转移公式

正确顺序应该是：

```text
dp 是什么？
↓
最后一步是什么？
↓
前面依赖谁？
↓
才写公式
```

---

## 5. 以为循环顺序都是固定的

最长回文子串告诉我：

> **循环顺序由依赖关系决定。**

0-1 背包告诉我：

> 倒序不是格式，而是为了防止重复使用当前元素。

---

## 6. 以为贪心就是眼前选最大的

其实：

> 局部最优必须能导向全局最优。

Jump Game II 的贪心对象不是：

> 当前跳得最远。

而是：

> 当前所有候选位置里，下一层覆盖最远。

---

## 7. 一看到 Top K 就排序

后来才建立：

```text
排序
堆
Quickselect
```

三个不同层次的选择。

核心问题是：

> **我到底需要多少排序信息？**

---

## 8. 做完一道题，只记住“答案”

这是我觉得最需要避免的一件事。

如果一道题做完以后，只记：

```text
这道题用 DFS。
```

其实价值很有限。

更应该继续问：

> 为什么是 DFS？

> 和之前哪道题是同一个模型？

> 如果把条件改一下，模型会不会变？

> 这道题真正新增的约束是什么？

当一道题能够被归入已有模型时，它才真正进入自己的知识体系。

---

# 二十四、Hot 100 最终应该被压缩成一张图

刷完 100 道题以后，我不希望脑海里留下 100 个孤立答案。

更希望最后变成这样：

```text
算法题
│
├── 数组 / 字符串
│   ├── 哈希
│   ├── 双指针
│   ├── 滑动窗口
│   ├── 前缀和
│   └── 二分
│
├── 链式结构
│   ├── 链表
│   ├── 快慢指针
│   └── Floyd
│
├── 树 / 图
│   ├── DFS
│   ├── BFS
│   ├── 递归
│   └── 回溯
│
├── 顺序结构
│   ├── 栈
│   ├── 单调栈
│   ├── 堆
│   └── Quickselect
│
└── 最优化问题
    ├── 贪心
    │
    └── DP
        ├── 一维 DP
        ├── 二维 DP
        ├── 字符串 DP
        ├── 0-1 背包
        └── 完全背包
```

以后面对一道新题：

> **不是从 100 道题里搜索最像的一道。**

而是：

> 先确定它属于哪个分支。

然后再进一步判断：

> 它相比基础模型增加了什么约束？

我觉得这才是刷 Hot 100 最终想获得的能力。

---

# 二十五、如果面试前只剩 30 分钟，我会怎么复习？

如果以后距离算法面试只剩半小时，我不会重新刷 100 道题。

甚至不会重新把这篇文章从头读一遍。

我只会做三件事。

---

## 前 10 分钟：恢复题型识别

快速过一遍：

```text
连续区间
→ 滑动窗口 / 前缀和

有序
→ 二分 / 双指针

所有方案
→ 回溯

树 / 图 / 连通性
→ DFS / BFS

最短步数
→ BFS

下一个更大
→ 单调栈

Top K
→ 堆 / Quickselect

环 / 中点
→ 快慢指针

最值 / 方案数 / 可达性
→ DP / 贪心

选或不选
→ 0-1 背包

无限使用
→ 完全背包
```

---

## 中间 10 分钟：默写核心骨架

不看答案写：

```text
滑动窗口
回溯
二分 lower_bound
链表反转
DFS
BFS
单调栈
0-1 背包
```

如果模板都能从空白编辑器写出来，基础手感基本就回来了。

---

## 最后 10 分钟：看自己的错误

重点提醒自己：

```text
下标还是数量？

切片有没有包含右端点？

二维数组有没有共享引用？

递归函数到底返回什么？

visited 是全局还是当前路径？

谁 add，谁 remove？

二分到底找哪个边界？

mid 有没有可能是答案？

dp 的状态定义是什么？

初始化是否符合定义？

状态依赖决定什么遍历顺序？

0-1 背包是不是倒序？
```

我越来越觉得：

> 面试前复习自己的错误，比重新看十道标准答案更有用。

因为真正容易让我在现场失败的，往往不是：

> “我不知道有这种算法。”

而是：

> **我知道这是二分，但边界写错了。**

> **我知道这是回溯，但 visited 恢复错了。**

> **我知道这是 DP，但 dp 的定义从一开始就偏了。**

---

# 写在最后

刷 Hot 100 的过程中，我有过很多现在看起来很基础的问题。

比如：

> 回溯以后为什么不会重新走同一个分支？

> 为什么这里用 `<=`，那里又用 `<`？

> 为什么 `dp[i][j]` 的 i 是前 i 个字符，却访问 `s[i-1]`？

> 为什么 0-1 背包倒序，而零钱兑换正序？

> 为什么树的递归返回深度，但答案却是直径？

这些问题单独拿出来看，好像都不复杂。

但真正把这些地方想明白以后，我才逐渐感觉：

> 自己不是在记一堆 LeetCode 答案，而是在建立一套算法语言。

回溯里的：

```text
当前层 → 下一层 → 恢复状态
```

二分里的：

```text
定义边界 → 保持不变量 → 缩小答案空间
```

DP 里的：

```text
定义状态 → 最后一步 → 状态转移 → 依赖顺序
```

DFS 里的：

```text
当前节点需要什么？
又应该返回什么给父节点？
```

其实都是同一件事：

> **把一个复杂问题拆成更小、结构更清晰的问题。**

所以刷完 Hot 100，并不是终点。

甚至 100 道题本身都不是最重要的。

真正重要的是：

> 下一次面对一道陌生题的时候，我能不能不依赖“见过”，而是通过结构，把它一步一步推出来。

如果半年以后我已经忘了大部分题目的代码，再重新打开这篇文章，我希望自己不需要重新记住 100 道题。

只需要重新找回：

> **看到问题以后，应该怎么想。**
